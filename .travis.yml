language: cpp

matrix:
  include:
  - os: linux
    dist: xenial
  - os: windows

before_install:
  - if [ "$TRAVIS_OS_NAME" = "linux" ]; then sudo apt-get install -y libglu1-mesa-dev; fi

script:
- script/cibuild

